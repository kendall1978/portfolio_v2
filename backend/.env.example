PORT=8080
DATABASE_URL=postgres://postgres:postgres@localhost:5432/kroberts_personal?sslmode=disable
JWT_SECRET=change-me-in-production
# 32-byte hex key for AES-256-GCM encryption of TOTP secrets
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
# BLS API key (optional, increases rate limit from 25 to 500 queries/day)
# Register at: https://data.bls.gov/registrationEngine/
BLS_API_KEY=
